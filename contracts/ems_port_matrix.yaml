# EMS Port Matrix — Local Rebuild v1
# No service start. Ports are configuration-only in this phase.

meta:
  version: "0.1.0-rebuild"
  phase: "phase1_local_rebuild"
  updated_at_utc: "2026-05-10T20:00:00+00:00"

ports:
  frontend:
    port: 3100
    purpose: "EMS Web UI"
    status: "configured_not_started"
  backend:
    port: 8100
    purpose: "EMS API"
    status: "configured_not_started"

forbidden_ports:
  - 3000
  - 3001
  - 3002
  - 3210
  - 5173
  - 4321
  - 8000

policies:
  start_policy: "disabled_in_rebuild_phase"
  validation_required: true
  bind_check_on_start: true
  fail_on_forbidden_port: true
